{% extends "layout.twig" %}

{% block title %}Accueil - The League{% endblock %}

{% block content %}
    <section class="top-home">
        <h1>Tout sur la league</h1>
        <p>Les players, les teams et l'actu des matchs</p>
    </section>
    
<<<<<<< HEAD

    {% if featured_team %}
    <section class="featured-team">
        <h2>{{ section_titles.teams }}</h2>
        <div class="team-card">
            <h3>{{ featured_team.name }}</h3>
            <p>{{ featured_team.description }}</p>
            <img src="{{ featured_team.logo.url }}" alt="{{ featured_team.logo.alt }}">
        </div>
    </section>
    {% endif %}
    
=======
    <!-- Section des équipes -->
    <section class="team-section">
        <h2>{{ section_titles.teams }}</h2>
        <div class="section-box">
            <div class="team-cards">
                {% for team in teams %}
                    {% include 'components/team-card.twig' with { team: team } %}
                {% endfor %}
            </div>
        </div>
    </section>

    <!-- Section des joueurs -->
>>>>>>> 4d9685004fb19b6831d63d537cd32ba2886a9987
    <section class="players-section">
    <h2>{{ section_titles.players }}</h2> 
    <div class="player-cards">
        {% for player in players %}
            {% include 'components/player-card.twig' with { player: player } %}
        {% endfor %}
    </div>
</section>


    <!-- Section des matchs -->
    <section class="match-section">
        <h2>{{ section_titles.matches }}</h2>
        <div class="section-box">  
            <div class="match-cards">
                {% for match in matches %}
                    {% include 'components/match-card.twig' with { match: match } %}
                {% endfor %}
            </div>
        </div>
    </section>

{% endblock %}
